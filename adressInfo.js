//let cities = [];
let idApt = [];
let adress = [];
let schedule = [];
let urlYM = [];
let aptekiKey = [];
let drugStores = [];
/* citiesTmp.push(row.get('city'));
cities = Array.from(new Set(citiesTmp));
citiesKeyboard = Markup
.keyboard([
    cities
]).oneTime().resize() */

        //console.log(cities)
        //const aptekiSheet = doc.sheetsByIndex[0];
        //const rows = await aptekiSheet.getRows();
        
        
        
        
        /* for (row of rows) {
            for (region of regions) {
                if (msg === region) {
                    citys.push(row.get('city'));
                    console.log(citys)
                }
                
            }
        } */
        
        /* ctx.reply(`Выберите город`, Markup
        .keyboard([
            citys
        ]).oneTime().resize(), {parse_mode: 'HTML'});
        for (let row of rows) {
            if(msg === cities) {
                city = row.get('city');
            }
            
        } */
            
            
             /* else if (msg === ) {
                await ctx.reply(`Адрес ${adress[i]}, расписание ${schedule[i]}, как проехать ${urlYM[i]}`);
            } */
            
        
            //aptekiKey.push(`Аптека № ${idApt[i]} на ${adress[i]}`);
            
      
        /* for (let key in citys) {
            if (citys[key] === msg) {
                drugStores.push(`Аптека №${idApt[key]} на ${adress[key]}`);
                await ctx.reply(`Выберите аптеку`, Markup.keyboard([
                    drugStores
                        ]).resize()
                );
        }
        }
        console.log(drugStores) 
        */
            

        /* bot.on(message('text'), async ctx => {
            let msg = ctx.message.text;
            for (let keys in drugStores) {
                if (drugStores[keys] === msg) {
                    await ctx.reply(`Адрес ${adress[keys]}, расписание ${schedule[keys]}, как проехать ${urlYM[keys]}`);
                }
                console.log(adress[keys])
            }
        }) */
        
        /* bot.hears(`Аптека № ${idApt[i]} на ${adress[i]}`, async (ctx) => {
            await ctx.reply(`Адрес ${adress[i]}, расписание ${schedule[i]}, как проехать ${urlYM[i]}`);
        }) */